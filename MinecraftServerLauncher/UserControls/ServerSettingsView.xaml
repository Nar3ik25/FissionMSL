<UserControl x:Class="MinecraftServerLauncher.UserControls.ServerSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MinecraftServerLauncher.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="950" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Grid>
            <TextBlock Text="Server Settings" FontSize="25" Margin="5,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
        </Grid>

        <Border Grid.Row="1" Margin="5" Padding="7" Background="{StaticResource Launcher.Window.GrayBrush3}" BorderBrush="{StaticResource Launcher.Window.GrayBrush6}" BorderThickness="4" CornerRadius="10">
            <ScrollViewer HorizontalScrollBarVisibility="Auto">
                <StackPanel Orientation="Vertical">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="serverName" Text="Placeholder name (this is a bug)" Margin="5,5,5,0" FontSize="30" VerticalAlignment="Top"/>
                        <TextBlock Text="Server Settings" Margin="5,5,5,0" FontSize="30" VerticalAlignment="Top"/>
                    </StackPanel>

                    <Rectangle Height="2" Margin="5,7,5,7" Fill="{StaticResource Launcher.Window.GrayBrush6}"/>
                    
                    <TextBlock Text="Server IP and Port" FontSize="23" Margin="5,0,0,0" HorizontalAlignment="Left"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBox x:Name="serverIPInput" MinWidth="200" Margin="5,4,0,0" Height="30" Text="127.0.0.1" FontSize="15" VerticalAlignment="Top" HorizontalAlignment="Stretch"/>
                        <TextBox x:Name="serverPortInput" MinWidth="100" Margin="5,4,0,0" Height="30" Text="28016" FontSize="15" VerticalAlignment="Top" HorizontalAlignment="Stretch"/>
                    </StackPanel>
                    <TextBlock x:Name="IPPortErrorText" Text="" Foreground="{StaticResource Launcher.RedBrush}" Margin="5,0,0,5" FontSize="15" VerticalAlignment="Top"/>
                    <Button Content="Copy IP and Port" Width="250" FontSize="20" Margin="5,5,0,10" Style="{StaticResource NormalButton}" HorizontalAlignment="Left" Click="CopyAddress_Button_Clicked"/>
                    
                    <Rectangle Height="2" Margin="5,7,5,7" Fill="{StaticResource Launcher.Window.GrayBrush6}"/>

                    <TextBlock Text="RAM Allocation (GB)" FontSize="23" Margin="5,0,0,0" HorizontalAlignment="Left"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBox x:Name="serverRamInput" MinWidth="50" Margin="5,4,0,0" Height="30" Text="4" FontSize="15" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                        <TextBlock Text="4 to 8 GB is recommended" FontSize="14" FontStyle="Italic" Margin="15,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                    </StackPanel>
                    <TextBlock x:Name="ramErrorText" Text="" Foreground="{StaticResource Launcher.RedBrush}" Margin="5,0,0,5" FontSize="15" VerticalAlignment="Top"/>

                    <TextBlock Text="Max Render Distance" FontSize="23" Margin="5,0,0,0" HorizontalAlignment="Left"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBox x:Name="serverRenderDistInput" MinWidth="50" Margin="5,4,0,0" Height="30" Text="10" FontSize="15" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                        <TextBlock Text="Values over 10 can cause performance issues" FontSize="14" FontStyle="Italic" Margin="15,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                    </StackPanel>
                    <TextBlock x:Name="renderDistErrorText" Text="" Foreground="{StaticResource Launcher.RedBrush}" Margin="5,0,0,5" FontSize="15" VerticalAlignment="Top"/>

                    <TextBlock Text="Allow Command Blocks" FontSize="23" Margin="5,5,0,0" HorizontalAlignment="Left"/>
                    <CheckBox x:Name="serverCommandBlockToggle" Content="Allow Command Blocks" IsChecked="True" FontSize="15" Margin="5,4,0,5"/>

                    <TextBlock Text="Allow PvP" FontSize="23" Margin="5,5,0,0" HorizontalAlignment="Left"/>
                    <CheckBox x:Name="serverPvPToggle" Content="Allow PvP" IsChecked="True" FontSize="15" Margin="5,4,0,5"/>

                    <Rectangle Height="2" Margin="5,15,5,7" Fill="{StaticResource Launcher.Window.GrayBrush6}"/>

                    <TextBlock Text="Server Path: " Margin="5,5,0,0" FontSize="23" VerticalAlignment="Top"/>
                    <TextBlock x:Name="serverPath" Text="Placeholder path (this is a bug)" Margin="5,5,5,5" FontSize="15" VerticalAlignment="Top"/>
                    <Button Content="Open File Location" Width="250" FontSize="20" Margin="5,5,0,10" Style="{StaticResource NormalButton}" HorizontalAlignment="Left" Click="OpenFolder_Button_Clicked"/>
                    
                    <Rectangle Height="2" Margin="5,7,5,7" Fill="{StaticResource Launcher.Window.GrayBrush6}"/>
                    
                    <TextBlock Text="WARNING: If you didn't set a seed, the world will be randomized!" Margin="5,10,0,0" FontSize="15" VerticalAlignment="Top"/>
                    <Button Content="Reset World" Width="250" FontSize="20" Margin="5,5,0,10" Style="{StaticResource NormalDeleteButton}" HorizontalAlignment="Left" Click="ResetWorld_Button_Clicked"/>
                    <TextBlock Text="WARNING: This will delete the server forever, you will lose everything!" Margin="5,10,0,0" FontSize="15" VerticalAlignment="Top"/>
                    <Button Content="Delete Server" Width="250" FontSize="20" Margin="5,5,0,10" Style="{StaticResource NormalDeleteButton}" HorizontalAlignment="Left" Click="DeleteServer_Button_Clicked"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="2">
            <Button Content="Save Settings" Width="170" FontSize="20" Margin="0,0,5,0" Style="{StaticResource NormalButton}" HorizontalAlignment="Right" Click="Save_Button_Clicked"/>
            <Button Content="Back to list" Width="170" FontSize="20" Margin="0,0,185,0" Style="{StaticResource NormalButton}" HorizontalAlignment="Right" Click="BacktoList_Button_Clicked"/>
        </Grid>
    </Grid>
</UserControl>
