<UserControl x:Class="MinecraftServerLauncher.UserControls.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MinecraftServerLauncher.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Grid>
            <TextBlock Text="Application Settings" FontSize="25" Margin="5,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
        </Grid>

        <Border Grid.Row="1" Margin="5" Padding="7" Background="{StaticResource Launcher.Window.GrayBrush3}" BorderBrush="{StaticResource Launcher.Window.GrayBrush6}" BorderThickness="4" CornerRadius="10">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Server Library Path (Only effects new servers)" FontSize="18" Margin="5,0,0,0" HorizontalAlignment="Left"/>
                <Grid Margin="5,4,0,0">
                    <TextBox x:Name="appServerPathInput" Margin="0,0,180,0" Height="30" Text="" FontSize="15" VerticalAlignment="Center" HorizontalAlignment="Stretch"/>
                    <Button Margin="0,0,90,0" Content="Browse" Width="80" FontSize="18" Style="{StaticResource NormalButton}" VerticalAlignment="Center" HorizontalAlignment="Right" Click="Browse_ServerLibraryPath_Button_Clicked"/>
                    <Button Content="Default" Width="80" FontSize="18" Style="{StaticResource NormalButton}" VerticalAlignment="Center" HorizontalAlignment="Right" Click="Default_ServerLibraryPath_Button_Clicked"/>
                </Grid>
                <TextBlock x:Name="serverPathErrorText" Text="" Foreground="{StaticResource Launcher.RedBrush}" Margin="5,0,0,5" FontSize="15" VerticalAlignment="Top"/>
                <TextBlock Text="Application Java Path" FontSize="18" Margin="5,0,0,0" HorizontalAlignment="Left"/>
                <Grid Margin="5,4,0,0">
                    <TextBox x:Name="appJavaPathInput" Margin="0,0,180,0" Height="30" Text="" FontSize="15" VerticalAlignment="Top" HorizontalAlignment="Stretch"/>
                    <Button Margin="0,0,90,0" Content="Browse" Width="80" FontSize="18" Style="{StaticResource NormalButton}" VerticalAlignment="Center" HorizontalAlignment="Right" Click="Browse_JavaPath_Button_Clicked"/>
                    <Button Content="Default" Width="80" FontSize="18" Style="{StaticResource NormalButton}" VerticalAlignment="Center" HorizontalAlignment="Right" Click="Default_JavaPath_Button_Clicked"/>
                </Grid>
                <TextBlock x:Name="appJavaPathErrorText" Text="" Foreground="{StaticResource Launcher.RedBrush}" Margin="5,0,0,5" FontSize="15" VerticalAlignment="Top"/>
                <Rectangle Height="2" Margin="5,7,5,7" Fill="{StaticResource Launcher.Window.GrayBrush6}"/>
                <TextBlock Text="About Fission MSL" FontSize="30" Margin="5,0,0,10" HorizontalAlignment="Left"/>
                <TextBlock Text="Made by Kieran Kelly" FontSize="18" Margin="5,0,0,0" HorizontalAlignment="Left"/>
                <TextBlock x:Name="versionText" Text="Application Version NULL" FontSize="18" Margin="5,0,0,0" HorizontalAlignment="Left"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2">
            <Button Content="Apply Settings" Width="160" FontSize="20" Margin="0,0,5,0" Style="{StaticResource NormalButton}" HorizontalAlignment="Right" Click="ApplySettings_Button_Clicked"/>
        </Grid>
    </Grid>
</UserControl>
