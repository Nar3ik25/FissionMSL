<UserControl x:Class="MinecraftServerLauncher.UserControls.UpdatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MinecraftServerLauncher.UserControls"
             xmlns:vm="clr-namespace:MinecraftServerLauncher.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:LoadChangeLogs/>
    </UserControl.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid>
            <TextBlock Text="Updates" FontSize="25" Margin="5,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
        </Grid>

        <Border Grid.Row="1" Margin="5" Padding="7" Background="{StaticResource Launcher.Window.GrayBrush3}" BorderBrush="{StaticResource Launcher.Window.GrayBrush6}" BorderThickness="4" CornerRadius="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="90"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="200"/>
                    </Grid.ColumnDefinitions>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="40"/>
                            <RowDefinition Height="25"/>
                            <RowDefinition Height="25"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <TextBlock x:Name="topText" Text="No updates found." FontSize="25" Margin="10,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        <TextBlock x:Name="versionText" Text="Installed Version: (Version)" Grid.Row="1" FontSize="15" Margin="10,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        <TextBlock x:Name="latestVersionText" Text="Latest Version: Checking..." Grid.Row="2" FontSize="15" Margin="10,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                    </Grid>
                    <Button x:Name="updateButton" IsEnabled="False" Grid.Column="1" Content="Update" Width="180" FontSize="30" Margin="0,0,5,0" Style="{StaticResource NormalButton}" HorizontalAlignment="Right" VerticalAlignment="Center" Click="Update_Button_Clicked"/>
                </Grid>
                <ProgressBar x:Name="progressBar" Margin="5" Grid.Row="1"/>
                <Grid Grid.Row="2">
                    <ListBox x:Name="changelogListBox" ItemsSource="{Binding ChangeLogs}" SelectedIndex="0" Grid.Row="1" Margin="5" Padding="7">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem" BasedOn="{StaticResource ListBoxItemStyle2}">
                                <EventSetter Event="MouseDoubleClick" Handler="OnListBoxItem_Mouse_DoubleClick"/>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                        <StackPanel Orientation="Vertical">
                                            <TextBlock x:Name="changelogName" Text="{Binding Name}" Margin="5,5,5,0" FontSize="25" VerticalAlignment="Top"/>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock x:Name="changesText" Text="Double click to read changes." Margin="5,0,10,5" FontSize="15" VerticalAlignment="Bottom"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <TextBlock x:Name="loadingText" Text="Loading..." FontSize="30" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
