<UserControl x:Class="MinecraftServerLauncher.UserControls.ServerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MinecraftServerLauncher.UserControls"
             xmlns:vm="clr-namespace:MinecraftServerLauncher.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:LoadServers/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <Geometry x:Key="ArrowRight">M 0 0 L 10 10 L 0 20 Z</Geometry>
        <Geometry x:Key="MagnifierIconGeometry">M44,12 C32,12 22,22 22,34 22,46 32,56 44,56 56,56 66,46 66,34 66,22 56,12 44,12z M44,0 C63,0 78,15 78,34 78,53 63,68 44,68 40,68 36.5,67.5 33,66 L32.5,66 14,90 0,79.5 18,55.5 17,55 C13,49 10,42 10,34 10,15 25,0 44,0z</Geometry>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid>
            <TextBlock Text="Server List" FontSize="25" Margin="5,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
            <Button Content="Create Server" Width="150" FontSize="20" Margin="0,0,5,0" Style="{StaticResource NormalButton}" HorizontalAlignment="Right" Click="AddServer_Button_Clicked"/>
            <Button Content="Refresh List" Width="150" FontSize="20" Margin="0,0,165,0" Style="{StaticResource NormalButton}" HorizontalAlignment="Right" Click="RefreshList_Button_Clicked"/>
        </Grid>

        <ListView x:Name="serversListBox" ItemsSource="{Binding Servers}" SelectedIndex="0" Grid.Row="1" Margin="5" Padding="7">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem" BasedOn="{StaticResource ListViewItemStyle2}">
                    
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                            <Border Width="55" Height="55" Margin="1,0,5,0" CornerRadius="2.5" Background="{StaticResource Launcher.Window.GrayBrush3}">
                                <Image Source="{Binding ServerIcon}" Width="50" Height="50"/>
                            </Border>
                            <Button DataContext="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Margin="5" Width="50" Height="50" FontSize="25" Style="{StaticResource PlayButton}" Background="{StaticResource Launcher.GreenBrush}" Click="Server_LaunchButton_Clicked">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="{StaticResource ArrowRight}" Width="20" Height="25" Fill="White" Stretch="Fill" />
                                </StackPanel>
                            </Button>
                            <StackPanel Orientation="Vertical">
                                <TextBlock x:Name="serverName" Text="{Binding ServerName}" Margin="5,5,5,0" FontSize="25" VerticalAlignment="Top"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock x:Name="serverLastUsed" Text="{Binding LastRan}" Margin="5,0,5,5" FontSize="15" VerticalAlignment="Bottom"/>
                                    <Rectangle Margin="2" Fill="{StaticResource Launcher.Window.GrayBrush6}" Width="2"/>
                                    <TextBlock x:Name="serverVersion" Text="{Binding ServerVersion}" Margin="5,0,5,5" FontSize="15" VerticalAlignment="Bottom"/>
                                    <Rectangle Margin="2" Fill="{StaticResource Launcher.Window.GrayBrush6}" Width="2"/>
                                    <TextBlock x:Name="serverGamemode" Text="{Binding ServerGamemodeFull}" Margin="5,0,5,5" FontSize="15" VerticalAlignment="Bottom"/>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button DataContext="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Margin="5" Width="50" Height="50" FontSize="25" Style="{StaticResource PlayButton}" Background="{StaticResource Launcher.BlueBrush}" Click="Server_SettingsButton_Clicked">
                                <StackPanel Orientation="Horizontal">
                                    <Image Source="\Dictionaries\GearIcon.png" Width="30" Height="30"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock x:Name="noServerText" Text="You have no Fission MSL servers." FontSize="30" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center"/>
    </Grid>
</UserControl>
