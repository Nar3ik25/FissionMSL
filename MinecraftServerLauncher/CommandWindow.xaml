<Window x:Class="MinecraftServerLauncher.CommandWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MinecraftServerLauncher"
        mc:Ignorable="d" Style="{StaticResource LauncherDialogStyle}" WindowStartupLocation="CenterOwner"
        Title="Command Window" MinHeight="480" MinWidth="600" Height="480" Width="600">
    <Window.Resources>
        <Geometry x:Key="ArrowRight">M 0 0 L 10 10 L 0 20 Z</Geometry>
        <Geometry x:Key="MagnifierIconGeometry">M44,12 C32,12 22,22 22,34 22,46 32,56 44,56 56,56 66,46 66,34 66,22 56,12 44,12z M44,0 C63,0 78,15 78,34 78,53 63,68 44,68 40,68 36.5,67.5 33,66 L32.5,66 14,90 0,79.5 18,55.5 17,55 C13,49 10,42 10,34 10,15 25,0 44,0z</Geometry>
        <Geometry x:Key="PlusIcon">M0.5,0 L0.5,1 M0,0.5 L1,0.5</Geometry>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="80"/>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Server Command Scripts" FontSize="30" Margin="10,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
        <Border Padding="3" Grid.Row="1" Margin="5" Background="{StaticResource Launcher.Window.GrayBrush4}" BorderThickness="4" CornerRadius="10" BorderBrush="{StaticResource Launcher.Window.GrayBrush6}">
            <Grid>
                <StackPanel Orientation="Horizontal" Visibility="Hidden">
                    <Button DataContext="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Margin="3" Width="50" Height="50" FontSize="25" Style="{StaticResource PlayButton}" Background="{StaticResource Launcher.GreenBrush}" Click="AddScript_Button_Clicked">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{StaticResource PlusIcon}" Width="35" Height="35" Stroke="White" StrokeThickness="7" Stretch="Fill" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
        <Border Padding="3" Grid.Row="2" Margin="5" Background="{StaticResource Launcher.Window.GrayBrush4}" BorderThickness="4" CornerRadius="10" BorderBrush="{StaticResource Launcher.Window.GrayBrush6}">
            <Grid>
                <TextBlock Text="WIP (Coming soon!)" FontSize="30" Margin="10,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Grid>
        </Border>
        <Button x:Name="cancelButton" Grid.Row="3" Content="Back" Width="140" Margin="6,0,0,0" HorizontalAlignment="Left" FontSize="20" Style="{StaticResource NormalButton}" Click="Back_Button_Clicked"/>
    </Grid>
</Window>
